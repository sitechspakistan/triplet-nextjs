export default function TextSec({ heading }) {
    return (
        <section>
            <div className="container">
                <h2 className="text-center">{heading}</h2>

            </div>
        </section>
    )
}